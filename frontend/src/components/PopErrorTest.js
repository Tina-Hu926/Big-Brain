// import React from 'react';
// import { shallow } from 'enzyme';
// // import renderer from 'react-test-renderer';
// import { PopError } from './PopError';

// describe('PopError component', () => {
//   it('triggers onclick event handler when clicked', () => {
//     const onclick = jest.fn();
//     const closeButton = shallow(<PopError onclick={close} />);
//     closeButton.find('close-butt').simulated('click');
//     expect(close).toHaveBeenCalledTimes(1);
//   });
//   it('chick include two typograpyh', () => {
//     expect(wrapper.find('Typography'))
//   });
// });
